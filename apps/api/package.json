{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "poetry install",
    "dev": "poetry run python run.py",
    "generate-requirements": "poetry export --without-hashes > requirements.txt"
  }
}
